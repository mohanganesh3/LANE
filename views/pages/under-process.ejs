<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - LANE Carpool</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .verification-container {
            max-width: 600px;
            width: 90%;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            animation: slideUp 0.6s ease;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .verification-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 40px 30px;
            text-align: center;
            color: white;
        }

        .verification-icon {
            width: 100px;
            height: 100px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 50px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
        }

        .verification-header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 700;
        }

        .verification-header p {
            margin: 10px 0 0;
            opacity: 0.9;
            font-size: 16px;
        }

        .verification-body {
            padding: 40px 30px;
        }

        .status-badge {
            display: inline-block;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
            margin-bottom: 20px;
        }

        .status-pending {
            background: #fef3c7;
            color: #92400e;
        }

        .status-review {
            background: #dbeafe;
            color: #1e40af;
        }

        .status-rejected {
            background: #fee2e2;
            color: #991b1b;
        }

        .verification-message {
            font-size: 16px;
            line-height: 1.8;
            color: #4b5563;
            margin-bottom: 30px;
        }

        .verification-steps {
            background: #f9fafb;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
        }

        .verification-steps h3 {
            margin: 0 0 15px;
            font-size: 18px;
            color: #1f2937;
        }

        .step {
            display: flex;
            align-items: start;
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid #e5e7eb;
        }

        .step:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }

        .step-number {
            width: 30px;
            height: 30px;
            background: #667eea;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 14px;
            flex-shrink: 0;
            margin-right: 15px;
        }

        .step-content {
            flex: 1;
        }

        .step-title {
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 5px;
        }

        .step-description {
            font-size: 14px;
            color: #6b7280;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }

        .btn {
            flex: 1;
            min-width: 150px;
            padding: 14px 24px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 15px;
            text-decoration: none;
            text-align: center;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }

        .btn-secondary {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
        }

        .btn-secondary:hover {
            background: #f3f4f6;
        }

        .support-info {
            text-align: center;
            margin-top: 30px;
            padding-top: 30px;
            border-top: 1px solid #e5e7eb;
        }

        .support-info p {
            color: #6b7280;
            font-size: 14px;
            margin: 0 0 10px;
        }

        .support-info a {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
        }

        .support-info a:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .verification-container {
                width: 95%;
                margin: 20px;
            }

            .verification-header {
                padding: 30px 20px;
            }

            .verification-body {
                padding: 30px 20px;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="verification-container">
        <div class="verification-header">
            <div class="verification-icon">
                <% if (status === 'PENDING') { %>
                    <i class="fas fa-clock"></i>
                <% } else if (status === 'IN_REVIEW') { %>
                    <i class="fas fa-hourglass-half"></i>
                <% } else if (status === 'REJECTED') { %>
                    <i class="fas fa-exclamation-circle"></i>
                <% } else { %>
                    <i class="fas fa-info-circle"></i>
                <% } %>
            </div>
            <h1><%= title %></h1>
            <p>Your rider verification is being processed</p>
        </div>

        <div class="verification-body">
            <div style="text-align: center; margin-bottom: 20px;">
                <% if (status === 'PENDING') { %>
                    <span class="status-badge status-pending">
                        <i class="fas fa-clock"></i> Verification Pending
                    </span>
                <% } else if (status === 'IN_REVIEW') { %>
                    <span class="status-badge status-review">
                        <i class="fas fa-hourglass-half"></i> Under Review
                    </span>
                <% } else if (status === 'REJECTED') { %>
                    <span class="status-badge status-rejected">
                        <i class="fas fa-times-circle"></i> Verification Rejected
                    </span>
                <% } %>
            </div>

            <% if (status === 'PENDING') { %>
                <div class="verification-message">
                    <p>Thank you for submitting your verification documents! Our team is currently reviewing your application.</p>
                    <p>This process typically takes <strong>24-48 hours</strong>. We'll notify you via email and SMS once your verification is complete.</p>
                </div>

                <div class="verification-steps">
                    <h3><i class="fas fa-list-check"></i> What happens next?</h3>
                    
                    <div class="step">
                        <div class="step-number">1</div>
                        <div class="step-content">
                            <div class="step-title">Document Review</div>
                            <div class="step-description">Our team verifies your license, vehicle registration, and insurance documents</div>
                        </div>
                    </div>
                    
                    <div class="step">
                        <div class="step-number">2</div>
                        <div class="step-content">
                            <div class="step-title">Background Check</div>
                            <div class="step-description">We conduct a safety and compliance check to ensure rider standards</div>
                        </div>
                    </div>
                    
                    <div class="step">
                        <div class="step-number">3</div>
                        <div class="step-content">
                            <div class="step-title">Approval & Activation</div>
                            <div class="step-description">Once approved, you'll receive confirmation and can start posting rides</div>
                        </div>
                    </div>
                </div>

            <% } else if (status === 'IN_REVIEW') { %>
                <div class="verification-message">
                    <p>Your documents are currently being reviewed by our verification team.</p>
                    <p>We're working hard to complete the review as quickly as possible. You'll receive an update within the next <strong>24 hours</strong>.</p>
                </div>

            <% } else if (status === 'REJECTED') { %>
                <div class="verification-message">
                    <p>Unfortunately, we couldn't verify your documents at this time. This could be due to:</p>
                    <ul style="margin: 15px 0; padding-left: 20px;">
                        <li>Unclear or incomplete document images</li>
                        <li>Expired documents</li>
                        <li>Mismatched information</li>
                        <li>Missing required documents</li>
                    </ul>
                    <p>Please review your documents and submit them again with correct information.</p>
                </div>

            <% } else { %>
                <div class="verification-message">
                    <p>Your rider verification is currently being processed. Please check back later for updates.</p>
                </div>
            <% } %>

            <div class="action-buttons">
                <a href="/user/dashboard" class="btn btn-primary">
                    <i class="fas fa-home"></i> Go to Dashboard
                </a>
                <% if (status === 'REJECTED') { %>
                    <a href="/user/verification" class="btn btn-secondary">
                        <i class="fas fa-redo"></i> Resubmit Documents
                    </a>
                <% } else { %>
                    <a href="/user/profile" class="btn btn-secondary">
                        <i class="fas fa-user"></i> View Profile
                    </a>
                <% } %>
            </div>

            <div class="support-info">
                <p><i class="fas fa-question-circle"></i> Need help with verification?</p>
                <a href="/support/contact"><i class="fas fa-envelope"></i> Contact Support</a>
                <span style="margin: 0 10px; color: #d1d5db;">|</span>
                <a href="/support/verification-faq"><i class="fas fa-book"></i> Verification FAQ</a>
            </div>
        </div>
    </div>

    <script>
        // Auto-refresh page every 5 minutes if pending
        <% if (status === 'PENDING' || status === 'IN_REVIEW') { %>
        setTimeout(() => {
            window.location.reload();
        }, 5 * 60 * 1000); // 5 minutes
        <% } %>
    </script>
</body>
</html>
